* Support .zig/config - author
* zig diff (working tree against commit or HEAD)
* .zignore
* zig checkout <commit>
* branches
* resolve (rev-parse)
* tags
* convert to swift pm project
* unit tests


* DONE Refactor commit into its own class
* DONE CommitView / TreeishIterator should return Commits, not Treeishe
